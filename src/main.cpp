#include "LidarDriver.h";
#include <iostream>
#include <vector>

int main()
{
	LidarDriver::LidarDriver a = LidarDriver::LidarDriver();
	
	std::vector<double> lettura;
	for(...)
	{
		
	}
	new_scan();
	
	return 0;
}